import { View, Text } from "react-native";

export default function CrearScreen() {
    return (
        <View style={{flex:1, justifyContent:'center', alignItems:'center', backgroundColor:'#969aa8'}}>
            <Text style={{fontSize:20}}>Crear</Text>
        </View>
    );
}